# KPL BP 推荐 Agent 课程项目报告

## 摘要

本项目面向王者荣耀 KPL 职业联赛中的 BP（Ban/Pick）环节，尝试构建一个能够辅助完成英雄禁选决策的智能 Agent。系统利用 KPL 历史 BP 数据、选手对局数据、英雄赛季统计数据和人工整理的英雄标签数据，在当前 BP 局面下推荐下一手应该 ban 或 pick 的英雄，并结合云端大模型给出阵容优劣、游戏思路和战术解释。

项目的核心不是简单地做一个“英雄胜率排行榜”，而是把 BP 看作一个动态博弈问题：每一步选择都会改变后续可选英雄、双方阵容结构和全局 BP 资源。为此，项目设计了“职业 BP 行为学习 + 阵容价值评估 + 队伍选手强度校正 + 全局 BP 资源管理 + 大模型解释与重排序”的整体方案。

面向课程展示，本报告重点介绍项目的设计初衷、数据特点、计算方法、模型思路、创新点、效果评估和未来优化方向，尽量减少对具体代码结构的依赖，使不熟悉代码的同学也能理解项目的核心思想。

## 1. 项目背景

### 1.1 什么是 BP

BP 是 Ban/Pick 的缩写，也就是比赛开始前双方进行英雄禁用和选择的过程。在王者荣耀职业比赛中，BP 往往直接影响一局比赛的战术方向。

一个好的 BP 需要同时考虑：

- 当前版本哪些英雄强势。
- 己方阵容是否完整。
- 敌方阵容有什么威胁。
- 己方选手是否擅长某些英雄。
- 是否需要封锁对方核心体系。
- 当前是单局还是 BO3/BO5/BO7 全局 BP。
- 后续局是否还需要保留强势英雄。

因此，BP 不是简单地选择胜率最高的英雄，而是一个多因素、多阶段、强对抗的决策过程。

### 1.2 为什么这个问题值得研究

KPL 职业比赛中的 BP 具有明显的数据分析价值：

- BP 数据具有清晰的时间顺序，适合建模为序列决策问题。
- 职业队伍的选择反映了版本理解、战术风格和英雄优先级。
- 选手数据可以帮助分析“阵容强弱”和“队伍实力”的区别。
- 全局 BP 规则使英雄资源管理变得重要，具有博弈特征。
- 大模型可以补充纯数值模型缺少的解释能力。

本项目希望通过数据和模型回答一个问题：

> 如果我处在一个具体 BP 局面中，下一手最合理的 ban 或 pick 是什么？为什么？

## 2. 项目设计初衷

### 2.1 不做简单胜率表

很多初步的 BP 分析容易退化成“哪个英雄胜率高就推荐哪个英雄”。但在职业 BP 中，这种方式是不够的。

例如：

- 某个英雄胜率高，可能是因为强队经常使用它。
- 某个英雄胜率低，可能是因为弱队被迫拿它补阵容。
- 某个英雄当前很强，但如果已经有同类型英雄，继续选择可能导致阵容失衡。
- 在 BO5 全局 BP 的第一局，直接消耗最强英雄未必是最优策略。

因此，项目从一开始就不把“英雄胜率”作为唯一依据，而是希望综合职业行为、阵容结构、队伍强度和赛制规则。

### 2.2 解决“强队因素”对模型的干扰

项目中遇到的一个重要问题是：

> 强队即使拿到一般阵容，也可能靠实力赢；弱队即使拿到不错阵容，也可能因为执行力不足输掉比赛。

如果直接用比赛胜负训练阵容强度，模型可能会产生错误结论：

```text
强队赢了 -> 模型以为阵容一定强
弱队输了 -> 模型以为阵容一定弱
```

为了减少这种影响，项目在阵容价值模型中引入了队伍和选手强度校正。模型会先估计队伍和选手本身的基础实力，再从胜负结果中提取更接近“阵容本身价值”的残差信号。

### 2.3 适应小样本和版本变化

KPL 数据有一个特点：同一版本的数据才真正有参考价值。英雄调整后，旧版本数据可能失效。

这意味着项目不能简单地使用几年所有数据训练一个复杂深度模型。因为：

- 可用的当前版本数据量有限。
- 深度神经网络容易过拟合历史比赛。
- 职业 BP 受版本影响很大。
- 复杂模型不容易解释。

因此，本项目选择更适合小样本的建模方式：

- 用频率模型学习职业 BP 行为。
- 用英雄统计作为先验。
- 用浅层搜索模拟后续局面。
- 用云端大模型增强解释和重排序。

## 3. 数据来源与数据特点

项目主要使用四类数据。

### 3.1 BP 顺序数据

BP 数据记录了每一局比赛中每一步禁用或选择的英雄。

这类数据的特点是：

- 有明确的顺序。
- 每一步都有当前阵营。
- 每一步都有动作类型，ban 或 pick。
- 每一步都会改变后续可选空间。

当前数据中：

| 指标 | 数值 |
|---|---:|
| BP 总行数 | 8330 |
| BP 单局数 | 419 |
| 完整 20 步 BP 局数 | 415 |
| ban 动作数 | 4150 |
| pick 动作数 | 4180 |
| 覆盖英雄数 | 103 |

这说明数据规模不算特别大，但结构较完整，大部分比赛都是标准完整 BP。

### 3.2 选手对局数据

选手数据记录每局比赛中 10 名选手分别使用的英雄和胜负结果。

它的作用非常关键：

- 判断 BP 中蓝方和红方分别对应哪支队伍。
- 判断每套阵容最终是否获胜。
- 估计队伍强度。
- 估计选手强度。
- 分析阵容胜负中有多少是阵容因素，有多少是队伍因素。

当前数据中：

| 指标 | 数值 |
|---|---:|
| 选手数据行数 | 4190 |
| 覆盖单局数 | 419 |
| 每局选手数 | 10 |
| 队伍数量 | 18 |
| 选手强度特征数 | 110 |

### 3.3 英雄赛季统计数据

英雄统计数据包括：

- 出场次数。
- 胜率。
- 平均 KDA。
- ban 率。
- pick 率。

这些数据被用作模型先验。

所谓“先验”，可以理解为模型在看具体 BP 局面之前，对英雄基础强度和版本热度的初始认识。

例如：

- ban 率高的英雄在 ban 阶段更容易被推荐。
- pick 率高、胜率较好的英雄在 pick 阶段会获得额外分数。
- 在样本较少时，英雄统计能让模型更加稳定。

### 3.4 英雄元数据与标签

项目还维护了一张 `hero_meta.csv`，用于描述英雄的战术属性。

包括：

- 主分路。
- 可选副分路。
- 职业定位。
- 伤害类型。
- 阵容标签。

例如，一个英雄可能具有：

```text
分路：游走
职业：辅助
伤害类型：法术
标签：开团、保护、控制、反打
```

这些标签主要用于阵容分析和云端 AI 提示词，使系统能够判断：

- 阵容是否缺少开团。
- 是否缺少前排。
- 是否缺少法术伤害。
- 是否过于偏后期。
- 是否缺少保护后排能力。

## 4. 问题建模

### 4.1 把 BP 看成序列决策

一局 BP 可以看成一个从第 0 手到第 19 手的序列。

在第 `t` 手时，系统已知：

- 当前是第几手。
- 当前轮到哪一方。
- 当前是 ban 还是 pick。
- 已经被 ban 的英雄。
- 蓝方已经 pick 的英雄。
- 红方已经 pick 的英雄。
- 当前赛制和局数。
- 全局 BP 中双方已经用过的英雄。

系统需要输出：

```text
下一手推荐英雄列表 = [英雄1, 英雄2, 英雄3, ...]
```

也就是说，本项目把 BP 推荐建模为：

```text
当前 BP 状态 -> 下一手英雄排序
```

### 4.2 状态表示

一个 BP 状态可以抽象为：

```text
State = {
  当前顺序,
  当前阵营,
  当前动作类型,
  已 ban 英雄,
  蓝方已选英雄,
  红方已选英雄,
  蓝方队伍,
  红方队伍,
  赛制模式,
  当前局数,
  全局已用英雄
}
```

这个状态既包含当前局面，也包含后续决策所需的约束。

### 4.3 推荐目标

推荐目标并不是简单预测“历史上这一手真实选了谁”，而是综合考虑：

- 职业队在类似局面下通常怎么选。
- 这个英雄加入后阵容价值是否提高。
- 对手下一手可能如何反制。
- 当前赛制是否需要保留高优先级英雄。
- 是否符合队伍画像和选手英雄池。

因此最终推荐分数可以理解为：

```text
综合推荐分 = 职业 BP 行为分 + 阵容价值分 + 赛制策略调整 + AI 重排序参考
```

## 5. 策略模型：学习职业 BP 行为

### 5.1 模型思想

策略模型的目标是模仿职业比赛的真实 BP 行为。

它回答的问题是：

> 在类似的 BP 局面下，职业队通常会选择什么英雄？

项目没有直接使用复杂神经网络，而是使用上下文频率模型。简单来说，就是统计历史比赛中不同局面下英雄出现的频率，并根据多个上下文加权求和。

### 5.2 为什么使用频率模型

频率模型虽然简单，但适合本项目：

- 数据量有限时更稳定。
- 结果可解释。
- 不容易过拟合。
- 能直接结合职业 BP 行为。
- 可以方便加入队伍偏好、英雄先验和上下文特征。

对课程项目而言，这种模型也更容易说明计算过程。

### 5.3 策略模型使用的特征

策略模型会考虑以下信息：

- 当前是第几手。
- 当前是 ban 还是 pick。
- 当前是蓝方还是红方。
- 己方已经选择了哪些英雄。
- 敌方已经选择了哪些英雄。
- 哪些英雄已经被 ban。
- 己方队伍历史上喜欢 pick 什么。
- 己方队伍历史上喜欢 ban 什么。
- 面对某个敌队时通常会 ban 什么。
- 英雄当前版本 pick 率、ban 率、胜率。
- 当前阵容形状，例如双方各有几个 pick。

### 5.4 计算方式

对每个合法英雄，模型会给一个初始分数，然后不断叠加来自不同特征的贡献。

可以简化表示为：

```text
PolicyScore(hero)
  = 英雄基础先验
  + 当前 BP 顺序贡献
  + 当前动作类型贡献
  + 己方已选英雄贡献
  + 敌方已选英雄贡献
  + 队伍偏好贡献
  + 英雄统计贡献
```

其中每一项都来自历史数据统计。

例如：

- 如果某英雄在第 4 手蓝方 pick 中经常出现，它在类似局面会加分。
- 如果某队历史上经常使用某英雄，该队行动时该英雄会加分。
- 如果面对某队时职业队经常 ban 某英雄，那么该英雄在 ban 阶段会加分。
- 如果某英雄在当前版本 ban 率很高，它在 ban 阶段会加分。

最后模型会把所有合法英雄按分数排序，得到候选列表。

## 6. 阵容价值模型：评估阵容本身是否合理

### 6.1 为什么需要阵容价值模型

只学习职业 BP 行为还不够。

因为职业队有时会因为战术隐藏、选手英雄池、训练赛准备等原因选择特定英雄。单纯模仿历史行为，可能无法判断一个英雄是否真正改善当前阵容。

因此项目加入阵容价值模型，用来回答：

> 当前英雄加入后，己方阵容是不是更合理？

### 6.2 队伍强度校正

阵容价值模型最重要的设计是队伍和选手强度校正。

计算思路如下：

1. 先根据历史胜负估计每支队伍和每名选手的基础强度。
2. 对每一局比赛计算“仅由队伍/选手强度带来的预期胜率”。
3. 用真实胜负减去这个预期胜率，得到残差信号。
4. 用残差信号学习英雄和阵容组合的价值。

可以用公式简化表示：

```text
真实结果 = 队伍实力影响 + 选手实力影响 + 阵容影响 + 随机因素
```

项目希望尽量分离出“阵容影响”。

因此：

```text
阵容残差 = 真实胜负 - 队伍/选手预期胜率
```

再用这个残差来更新英雄价值。

### 6.3 阵容价值包含哪些内容

当前模型会学习：

- 单个英雄对阵容胜负的影响。
- 己方英雄组合的协同效果。
- 敌我英雄对位的 matchup 效果。
- 英雄赛季统计带来的基础强度。

但在最终推荐时，系统默认不直接使用队伍强度来判断胜负，而是更关注阵容本身。

这是因为 BP 推荐应该帮助用户选出更好的阵容，而不是简单判断哪支队伍更强。

### 6.4 一个例子

假设一支强队使用阵容 A 赢了很多局。

如果不做校正，模型可能认为：

```text
阵容 A 非常强
```

但校正后模型会先问：

```text
这支强队本来就有多大概率赢？
```

如果强队本来预期胜率就很高，那么阵容 A 的额外贡献会被降低。

这样可以避免模型把强队实力错误归因给阵容本身。

## 7. 搜索与综合推荐

### 7.1 为什么需要搜索

BP 是连续过程。当前选择一个英雄，不仅影响当前阵容，还会影响下一手和后续多手。

例如：

- 当前抢一个核心输出，可能会暴露阵容缺少前排。
- 当前不 ban 某个英雄，对面下一手可能直接拿走。
- 当前选择某个强势英雄，可能导致后续分路难以补齐。

因此项目在策略模型和价值模型之上加入浅层搜索。

### 7.2 搜索思想

搜索过程可以简化为：

```text
对每个候选英雄：
  假设我方选择它
  模拟进入下一手
  假设对方会选择对我方最不利的方案
  评估后续阵容价值
```

这类似 minimax 思想：

- 我方行动时，希望最大化己方价值。
- 敌方行动时，假设敌方会最小化我方价值。

### 7.3 最终分数

本地 Agent 最终使用加权方式综合：

```text
最终分数 = 0.65 * 策略模型分 + 0.35 * 搜索后的阵容价值分
```

这样设计的原因是：

- 职业 BP 行为很重要，所以策略模型权重更高。
- 阵容价值能纠正部分不合理选择，所以保留较大比例。
- 小样本情况下，过度相信价值模型可能不稳定。

## 8. 全局 BP 与赛制策略

### 8.1 为什么全局 BP 不同于单局 BP

在单局 BP 中，目标通常是让当前局最优。

但在 BO3、BO5、BO7 全局 BP 中，英雄资源是跨局消耗的。某个英雄如果在前一局用过，后续局本方可能不能再使用。

因此，全局 BP 中需要考虑：

- 当前局是否值得消耗强势英雄。
- 后续还有几局。
- 是否应该保留高优先级英雄给关键局。
- 对方已经使用过哪些英雄，哪些英雄已经不能再被对方 pick。

### 8.2 支持的赛制

项目支持：

| 模式 | 含义 |
|---|---|
| 单局普通 BP | 只考虑当前局 |
| BO3 全局 BP | 三局两胜，英雄资源压力较小 |
| BO5 全局 BP | 五局三胜，资源管理更重要 |
| BO7 全局 BP | 七局四胜，资源管理最强 |
| BO5 巅峰对决 | 最后一局无 ban，双方盲选式 pick |
| BO7 巅峰对决 | 第七局无 ban，双方盲选式 pick |

### 8.3 资源保留策略

系统会对全局 BP 早期局的高优先级英雄加入“资源保留惩罚”。

直观理解：

```text
越早的局，后面剩余局数越多，越不应该随便消耗最强英雄。
```

项目中不同赛制的保留强度不同：

- BO3：保留压力较低。
- BO5：保留压力中等。
- BO7：保留压力最高。

因此，同样一个局面，在单局、BO3、BO5、BO7 下可能得到不同的推荐顺序。

### 8.4 巅峰对决

巅峰对决没有 ban，只进行 10 手 pick。

项目为巅峰对决单独设置了 pick 顺序：

```text
1, 2, 2, 1, 1, 2, 2, 1, 1, 2
```

此时模型会更重视阵容完整性、英雄熟练度和团战容错。

## 9. 英雄标签与阵容分析

### 9.1 为什么需要英雄标签

纯数字模型可以给出分数，但难以解释阵容为什么好或不好。

例如，模型可能推荐某个英雄，但用户还想知道：

- 这套阵容缺不缺开团？
- 有没有前排？
- 输出类型是否单一？
- 能不能打后期？
- 怕不怕对面切后排？

这些问题很难仅靠胜率回答，因此项目引入英雄标签。

### 9.2 分析维度

本地阵容分析会统计：

- 分路完整性。
- 职业结构。
- 物理/法术/真实伤害比例。
- 开团能力。
- 保护能力。
- 消耗能力。
- 后期能力。
- 单带能力。
- 前排承伤能力。

### 9.3 分析规则示例

例如：

```text
如果开团标签数量 >= 2：
  认为阵容具备主动开团能力

如果保护标签数量 >= 2：
  认为阵容适合围绕核心输出打反打

如果法术伤害数量 = 0：
  提示阵容缺少法术伤害，容易被堆物抗

如果前排标签数量 = 0：
  提示正面团战容错较低
```

这种规则分析虽然不如大模型灵活，但它稳定、快速、可解释，并且不依赖外部 API。

## 10. 大模型协同设计

### 10.1 为什么引入大模型

本地模型擅长：

- 快速返回推荐。
- 基于历史数据给分。
- 保持输出稳定。
- 执行规则和过滤。

但本地模型不擅长：

- 用自然语言解释复杂阵容。
- 综合队伍画像给出战术建议。
- 像教练一样分析前中后期打法。
- 给出“为什么不选另一个高分英雄”的解释。

因此项目引入云端大模型，形成双层系统：

```text
本地模型负责候选生成
云端大模型负责解释、分析和重排序
```

### 10.2 云端 AI 复盘

云端 AI 分析会接收：

- 当前 BP 状态。
- 本地模型推荐。
- 双方已选阵容。
- 英雄标签分析。
- 当前赛制。
- 全局已用英雄。
- 队伍画像。
- BP 经验知识库。

然后输出：

- 当前 BP 关键矛盾。
- 蓝方阵容强点和短板。
- 红方阵容强点和短板。
- 下一手建议。
- 全局 BP 资源判断。
- 进入游戏后的前中后期打法。
- 风险提醒。

### 10.3 LLM 重排序

本地模型会先给出 Top-10 候选。

LLM 重排序会进一步考虑：

- 队伍画像。
- 选手擅长英雄。
- 选手回避英雄。
- 当前阵容短板。
- 对方阵容威胁。
- 全局 BP 资源。
- 本地模型分数。

最终返回一个更偏“教练视角”的排序。

### 10.4 为什么不是完全依赖大模型

项目没有让大模型直接凭空推荐英雄，原因是：

- 大模型可能编造不存在的数据。
- 大模型不一定严格遵守 KPL 当前 BP 规则。
- 大模型可能不知道当前数据集中的英雄池和版本情况。
- 大模型响应慢，成本高，不适合每一步都完全依赖。

因此项目采用：

```text
本地模型约束范围，大模型负责解释和二次判断
```

这比纯大模型方案更稳定。

## 11. 前端交互设计

### 11.1 设计目标

前端不是简单展示表格，而是希望模拟真实 BP 过程。

用户可以：

- 选择赛制。
- 选择当前局数。
- 输入队伍名称。
- 填写队伍和选手画像。
- 按分路选择英雄。
- 在每一步查看模型推荐。
- 调用 AI 重排序。
- 查看完整 AI 分析报告。

### 11.2 页面划分

当前前端分为三类页面：

- BP 模拟器：完成交互式 ban/pick。
- 英雄资料库：查看英雄元数据和标签。
- 阵容分析与云端 AI：展示完整 AI 报告。

这种拆分是为了避免所有信息挤在一个页面中。

尤其是 AI 返回内容通常很长，如果直接放在 BP 页面右侧，会导致页面不平衡。因此项目将 BP 页面中的 AI 分析改为短预览，完整报告放到独立宽版页面中。

## 12. 当前效果评估

### 12.1 策略模型指标

当前策略模型在测试集上的结果：

| 指标 | 数值 |
|---|---:|
| 测试 BP 样本数 | 1660 |
| Top-1 命中率 | 17.47% |
| Top-3 命中率 | 34.82% |
| Top-5 命中率 | 44.16% |
| 合法英雄覆盖率 | 100% |

Top-1 命中率不算高，但在 BP 推荐场景中是可以理解的。因为 BP 并没有唯一正确答案，同一个局面可能有多个合理选择。

因此 Top-5 更能反映模型是否能把职业队真实选择放进候选范围。

### 12.2 阵容价值模型指标

| 模型设置 | Accuracy | Brier |
|---|---:|---:|
| 不含队伍强度 | 46.99% | 0.2483 |
| 含队伍强度 | 59.04% | 0.2688 |

这个结果说明：

- 队伍和选手实力确实对胜负影响很大。
- 加入队伍强度后更容易预测胜负。
- 但 BP 推荐更关心阵容本身，所以推荐时默认不直接使用队伍强度。

换句话说：

```text
预测谁会赢 -> 队伍强度很重要
推荐怎么 BP -> 更应该关注阵容本身
```

### 12.3 项目结果的理解

当前系统已经能够：

- 在每一步给出合法候选。
- 根据不同赛制调整推荐。
- 对全局 BP 做资源管理。
- 用英雄标签解释阵容优劣。
- 用大模型生成教练式分析。
- 根据队伍画像重排序推荐。

但它仍是一个原型系统，不应被理解为绝对正确的职业 BP 教练。

## 13. 项目创新点

### 13.1 将 BP 建模为动态博弈而不是静态排序

项目没有简单输出英雄强度榜，而是根据当前 BP 状态逐步推荐。

这更接近真实 BP 场景。

### 13.2 引入队伍和选手强度校正

项目明确处理了“强队因素”对阵容价值训练的干扰。

这是本项目相对普通胜率模型的重要改进。

### 13.3 适合小样本版本数据的建模路线

项目没有盲目使用复杂深度学习，而是选择：

- 频率模型。
- 统计先验。
- 残差价值学习。
- 浅层搜索。
- 大模型辅助。

这种路线更适合版本有效期短、数据量有限的电竞数据。

### 13.4 支持全局 BP 资源管理

项目区分单局、BO3、BO5、BO7，并根据剩余局数调整高优先级英雄推荐。

这使系统更符合 KPL 系列赛实际规则。

### 13.5 本地模型与大模型协同

项目不是简单调用大模型，而是让本地模型先生成有约束的候选，再由大模型解释和重排序。

这种架构兼顾：

- 稳定性。
- 可解释性。
- 灵活性。
- 交互体验。

### 13.6 英雄标签驱动的可解释阵容分析

通过人工维护英雄元数据，系统能够解释阵容结构，而不只是返回分数。

这让项目更适合教学展示和用户理解。

## 14. 局限性

### 14.1 数据量有限

当前完整 BP 数据约 415 局，对于复杂模型来说仍然偏少。

### 14.2 版本变化影响大

英雄强度会随版本变化，因此旧数据不一定适用于新版本。

### 14.3 阵容价值建模仍较困难

比赛胜负受到执行、临场决策、选手状态等很多因素影响，仅凭 BP 阵容很难准确预测。

### 14.4 英雄标签需要人工维护

如果标签不准确，阵容分析和大模型提示词都会受到影响。

### 14.5 云端 AI 依赖外部服务

大模型功能依赖 API Key、网络连接和模型服务可用性。

### 14.6 当前还不是强化学习自博弈系统

项目目前是监督学习、价值评估、浅层搜索和大模型辅助的组合，还没有实现真正的强化学习自博弈。

## 15. 后续优化方向

### 15.1 数据优化

后续可以增加：

- 更细的版本号。
- 英雄调整记录。
- 选手近期英雄池。
- 队伍近期状态。
- 局内经济差。
- 暴君/主宰控制数据。
- 一血、一塔等关键事件。

### 15.2 模型优化

可以尝试：

- 对不同版本分别训练模型。
- 加入时间衰减，让近期比赛权重更高。
- 使用 LightGBM 或 CatBoost 建模阵容价值。
- 为不同 BP 阶段训练不同策略模型。
- 对英雄 pair 和 matchup 做更强正则化。
- 引入选手英雄熟练度特征。

### 15.3 全局 BP 优化

未来可以进一步做跨局规划：

- 记录整个 BO 系列赛的英雄使用历史。
- 预测后续局可能需要的英雄资源。
- 为每局设置不同战略目标。
- 模拟“先输一局/先赢一局”后的 BP 策略变化。

### 15.4 前端优化

可以继续增加：

- 英雄头像。
- BP 动画。
- 推荐理由可视化。
- 多局系列赛管理。
- BP 报告导出。
- 队伍画像模板。
- 英雄标签编辑器。

## 16. 总结

本项目围绕 KPL 职业比赛 BP 场景，构建了一个结合数据建模和大模型分析的推荐 Agent。项目的核心价值在于，它没有把 BP 简化成英雄胜率排序，而是从职业 BP 行为、阵容价值、队伍选手强度、全局 BP 资源和自然语言解释多个层面进行综合建模。

从课程项目角度看，本项目体现了以下特点：

- 问题具有真实应用背景。
- 数据具有明确结构和时序特征。
- 模型设计考虑了小样本和偏差问题。
- 系统支持完整交互流程。
- 大模型不是替代本地模型，而是作为解释和重排序模块。
- 项目能够展示从数据、模型到前端应用的完整闭环。

当前系统仍有提升空间，但已经可以作为一个较完整的 KPL BP 智能推荐原型。它展示了如何把传统数据建模、规则系统、搜索算法和大模型能力结合起来，解决一个具有博弈性和业务背景的实际问题。
# KPL BP 推荐 Agent 项目报告

## 1. 项目概述

本项目是一个面向 KPL 王者荣耀职业联赛 BP 场景的智能推荐系统。项目的核心目标不是单纯预测比赛胜负，而是在给定当前 BP 局面、双方已 ban/pick 英雄、赛制模式、队伍信息与选手画像的情况下，给出下一手 ban 或 pick 的推荐，并通过本地模型与云端大模型结合的方式提供解释、重排序和战术分析。

当前系统已经从最初的离线推荐脚本扩展为一个完整的前后端应用，包含数据清洗、模型训练、本地推荐、全局 BP 规则、英雄元数据分析、队伍画像、云端 AI 复盘、LLM 推荐重排序和交互式 BP 模拟页面。

项目整体定位可以概括为：

- 面向 KPL 当前版本数据的小样本 BP 推荐 Agent。
- 结合职业 BP 历史、阵容价值、英雄强度先验和队伍/选手信息的辅助决策系统。
- 支持单局 BP、BO3/BO5/BO7 全局 BP，以及带巅峰对决的系列赛 BP 模式。
- 支持前端交互模拟 BP，并在每一步返回本地模型候选和云端 AI 分析。

## 2. 项目背景与问题定义

KPL BP 是一个高度复杂的序贯博弈问题。每一手 ban/pick 都会受到版本强度、英雄克制、阵容结构、战队风格、选手英雄池、全局 BP 资源管理和对手策略的共同影响。

传统的简单胜率排序无法直接解决 BP 推荐问题，原因包括：

- 英雄强度会随版本更新快速变化，历史长期数据不一定适用于当前版本。
- 职业 BP 并不是单局最优选择，还包含抢版本英雄、封锁对手体系、隐藏战术、给后续局保留资源等考虑。
- 比赛胜负受到队伍实力与选手状态影响，不能把胜负结果简单等同于阵容强弱。
- 数据规模有限，直接训练复杂神经网络或强化学习模型容易过拟合。
- BP 是动态过程，每一步选择都会改变后续可选空间和双方阵容结构。

因此，本项目采用了较稳健的工程路线：

```text
职业 BP 历史行为学习
        +
阵容价值评估
        +
英雄统计先验
        +
队伍/选手强度校正
        +
浅层搜索与赛制规则
        +
云端大模型解释与重排序
```

## 3. 项目目标

### 3.1 核心目标

项目的核心目标是训练并部署一个 KPL BP 推荐 Agent，使其能够在 BP 的每一步回答：

- 当前轮到哪一方进行 ban 或 pick？
- 当前还有哪些英雄合法可选？
- 本地模型最推荐哪些英雄？
- 推荐理由来自职业 BP 习惯、阵容价值还是全局 BP 资源管理？
- 当前阵容有什么强点、短板和游戏内打法？
- 在 BO3/BO5/BO7 全局 BP 中，是否应该保留高优先级英雄给后续局？

### 3.2 功能目标

当前项目已经实现以下功能：

- 使用 KPL BP 数据训练策略模型。
- 使用选手对局数据训练阵容价值模型。
- 使用英雄赛季统计作为小样本先验。
- 支持英雄元数据，包括分路、职业、伤害类型、阵容标签和多分路。
- 支持本地命令行推荐。
- 支持后端 API 推荐。
- 支持前端交互式 BP 模拟。
- 支持 BO3/BO5/BO7 全局 BP 与巅峰对决。
- 支持队伍画像输入，包括选手风格、状态、擅长英雄和回避英雄。
- 支持云端 AI 分析当前 BP 局面。
- 支持云端 LLM 对本地 Top-K 推荐进行重排序。
- 支持英雄资料库和阵容分析子页面。

## 4. 项目目录结构

当前项目主要文件结构如下：

```text
.
├── KPL_hero_2023_2026.csv
├── kpl_bp.csv
├── kpl_players.csv
├── hero_meta.csv
├── cloud_api_config.json
├── cloud_api_config.example.json
├── README.md
├── PROJECT_REPORT.md
├── docs/
│   └── BP_EXPERIENCE.md
├── kplbp/
│   ├── __init__.py
│   ├── agent.py
│   ├── analysis.py
│   ├── api.py
│   ├── bp_rules.py
│   ├── data.py
│   ├── models.py
│   ├── recommend.py
│   ├── schema.py
│   └── train.py
├── models/
│   └── kpl_bp_agent.json
├── reports/
│   ├── audit.json
│   └── metrics.json
└── web/
    └── index.html
```

### 4.1 数据文件

- `kpl_bp.csv`：职业比赛逐步 BP 数据。
- `kpl_players.csv`：每局双方选手、使用英雄、表现指标和胜负结果。
- `KPL_hero_2023_2026.csv`：英雄赛季级统计数据。
- `hero_meta.csv`：英雄位置、职业、伤害类型、标签和多分路元数据。

### 4.2 核心代码

- `kplbp/schema.py`：定义核心数据结构。
- `kplbp/data.py`：负责 CSV 加载、数据审计和训练样本构造。
- `kplbp/models.py`：实现策略模型和价值模型。
- `kplbp/agent.py`：组合策略模型、价值模型和浅层搜索。
- `kplbp/bp_rules.py`：实现 BO3/BO5/BO7、全局 BP、巅峰对决规则。
- `kplbp/analysis.py`：基于英雄标签做阵容结构分析。
- `kplbp/api.py`：提供后端 HTTP API 和前端静态资源服务。
- `kplbp/train.py`：训练入口。
- `kplbp/recommend.py`：命令行推荐入口。

### 4.3 前端与配置

- `web/index.html`：单文件前端应用，包含 BP 模拟器、英雄资料库、阵容分析页和云端 AI 交互。
- `cloud_api_config.json`：当前云端大模型配置。
- `cloud_api_config.example.json`：云端配置示例。
- `docs/BP_EXPERIENCE.md`：BP 经验知识库，会被注入云端 AI prompt。

## 5. 数据设计与数据质量

### 5.1 BP 数据

`kpl_bp.csv` 中每一行表示一次 BP 动作，关键字段包括：

- `match_id`：比赛 ID。
- `battle_id`：单局 ID。
- `order`：BP 顺序。
- `camp`：阵营，通常为 `1` 或 `2`。
- `type`：动作类型，`ban` 或 `pick`。
- `hero`：被 ban 或 pick 的英雄。

当前数据审计结果：

| 指标 | 数值 |
|---|---:|
| BP 行数 | 8330 |
| BP 单局数 | 419 |
| 完整 20 步 BP 局数 | 415 |
| 7 步 BP 局数 | 2 |
| 8 步 BP 局数 | 2 |
| ban 动作数 | 4150 |
| pick 动作数 | 4180 |
| 阵营 1 动作数 | 4163 |
| 阵营 2 动作数 | 4167 |
| 重复 order 局数 | 0 |
| 非单调 order 局数 | 0 |

该审计结果说明当前 BP 数据整体质量较好，绝大多数比赛是完整 20 步 BP，适合用于训练标准 BP 流程模型。

### 5.2 选手数据

`kpl_players.csv` 每一行表示某名选手在某一局中使用某个英雄的表现。关键字段包括：

- `match_id`
- `battle_id`
- `team`
- `player`
- `hero`
- `kill`
- `death`
- `assist`
- `kda`
- `gold`
- `win`

当前数据审计结果：

| 指标 | 数值 |
|---|---:|
| player 行数 | 4190 |
| players 中单局数 | 419 |
| 每局 player 行数 | 10 |
| BP 有但 players 无的局数 | 0 |
| players 有但 BP 无的局数 | 0 |
| 推断出的 camp-team 关系数 | 838 |

选手数据对项目非常关键，因为它用于：

- 推断 BP 中 `camp=1` 和 `camp=2` 分别是哪支队伍。
- 构建阵容胜负样本。
- 学习队伍强度和选手强度。
- 减少强队/强选手因素对阵容价值训练的干扰。

### 5.3 英雄统计数据

`KPL_hero_2023_2026.csv` 提供英雄赛季统计，字段包括：

- `league_id`
- `hero_name`
- `battle_count`
- `win_rate`
- `avg_kda`
- `ban_rate`
- `pick_rate`

当前数据中英雄统计行数为 271，覆盖英雄数为 103。

这些统计在模型中作为先验使用，尤其在样本量有限时可以提高稳定性。例如策略模型在计算先验分时会使用英雄 pick 率、ban 率和胜率。

### 5.4 英雄元数据

`hero_meta.csv` 是系统进行阵容结构分析和前端分类展示的重要文件。字段包括：

- `hero`：英雄名称。
- `lane`：主分路。
- `alt_lanes`：可选副分路。
- `role`：职业或功能定位。
- `damage_type`：主要伤害类型。
- `tags`：英雄标签，如开团、保护、消耗、后期、单带、切后排等。

元数据用于：

- 前端英雄按分路展示。
- 英雄资料库查询。
- 阵容分路完整性判断。
- 阵容伤害结构判断。
- 阵容标签分析。
- 云端 AI prompt 的上下文补充。

## 6. 核心数据结构

项目在 `kplbp/schema.py` 中定义了若干核心结构。

### 6.1 `BPState`

`BPState` 表示当前 BP 局面，包含：

- `match_id`
- `battle_id`
- `order`
- `camp`
- `action_type`
- `banned`
- `camp1_picks`
- `camp2_picks`
- `camp1_team`
- `camp2_team`

它还提供了几个辅助方法：

- `used_heroes()`：返回已经被使用的英雄，包括 ban 和双方 pick。
- `own_picks()`：根据当前行动阵营返回己方已选英雄。
- `enemy_picks()`：返回敌方已选英雄。
- `own_team()`：返回当前行动方队伍。
- `enemy_team()`：返回当前行动方敌队。
- `to_dict()`：转换为 API 可返回的字典。

### 6.2 `BPSample`

`BPSample` 是策略模型训练样本，由两部分构成：

- `state`：当前 BP 状态。
- `label`：真实下一手英雄。

策略模型的任务就是在给定 `state` 的情况下预测 `label`。

### 6.3 `LineupSample`

`LineupSample` 是阵容价值模型训练样本，包含：

- `battle_id`
- `camp`
- `own_picks`
- `enemy_picks`
- `win`
- `team`
- `enemy_team`
- `players`
- `enemy_players`

它用于学习阵容与胜负之间的关系，同时结合队伍和选手强度进行校正。

### 6.4 `HeroMeta`

`HeroMeta` 表示英雄元数据：

- `hero`
- `lane`
- `lanes`
- `role`
- `damage_type`
- `tags`

其中 `lanes` 支持一个英雄出现在多个分路中，例如某些英雄既可以打野也可以对抗路。

### 6.5 `TeamProfile` 与 `PlayerProfile`

前端支持用户填写队伍画像，包括：

- 队伍名称。
- 选手姓名。
- 选手分路。
- 选手打法风格。
- 选手状态。
- 擅长英雄。
- 回避英雄。
- 队伍整体风格。
- 队伍胜利条件偏好。

这些画像会通过 API 发送给后端，并被注入云端 AI 和 LLM 重排序 prompt。

## 7. 模型设计

当前模型由三部分组成：

```text
PolicyModel 策略模型
        +
ValueModel 阵容价值模型
        +
BPAgent 搜索与综合排序
```

### 7.1 策略模型 `PolicyModel`

策略模型的目标是学习职业比赛中真实 BP 行为，即“职业队在类似局面下通常会选择什么”。

它不是直接学习英雄绝对强弱，而是学习 BP 决策习惯。该设计适合小样本场景，因为频率模型比复杂神经网络更稳定，也更容易解释。

#### 7.1.1 输入

输入为 `BPState`，即当前 BP 状态。

#### 7.1.2 输出

输出为英雄排序列表：

```text
[(hero, probability_like_score), ...]
```

#### 7.1.3 使用的上下文特征

策略模型使用了多层上下文特征：

- 精确位置特征：`action_type + order + camp`
- 顺序特征：`action_type + order`
- 动作类型特征：`ban` 或 `pick`
- 当前行动方队伍偏好。
- 当前行动方特定顺序偏好。
- 针对敌队的 ban 偏好。
- 己方已选英雄带来的协同上下文。
- 敌方已选英雄带来的克制上下文。
- 已 ban 英雄后的后续选择上下文。
- 己方英雄 pair 特征。
- 敌方英雄 pair 特征。
- 当前阵容形状特征，包括己方 pick 数、敌方 pick 数、ban 数。
- 英雄赛季统计先验。

#### 7.1.4 队伍偏好

模型维护了三类队伍相关计数器：

- `team_pick_priors`：某队伍历史 pick 偏好。
- `team_ban_priors`：某队伍历史 ban 偏好。
- `target_ban_priors`：面对某敌队时常 ban 的英雄。

这些特征可以让模型在输入队伍名称时更贴近特定队伍风格。

#### 7.1.5 小样本优势

项目没有直接使用深度神经网络作为策略模型，主要原因是：

- 当前有效样本较少，复杂模型容易过拟合。
- BP 受版本影响极大，旧版本数据可能引入噪声。
- 频率模型可以自然利用职业 BP 行为模式。
- 特征可解释，便于调试。
- 与后续搜索和云端 AI 结合更灵活。

### 7.2 阵容价值模型 `ValueModel`

阵容价值模型用于评估一个阵容在排除队伍强度影响后是否更有价值。

它解决的是另一个问题：

> 如果一套阵容经常赢，是因为阵容本身强，还是因为使用它的队伍和选手强？

#### 7.2.1 队伍/选手强度校正

模型先从 `kpl_players.csv` 中估计：

- `team_strengths`
- `player_strengths`

然后计算仅由队伍和选手强度得到的预期胜率，再用真实胜负减去该预期，得到残差信号。英雄价值、英雄 pair 效果和 matchup 效果会主要从残差信号中学习。

这样可以减少如下错误：

```text
强队拿普通阵容赢了 -> 模型误以为阵容特别强
弱队拿优秀阵容输了 -> 模型误以为阵容很差
```

#### 7.2.2 当前使用方式

在 BP 搜索时，`BPAgent` 调用 `ValueModel.predict(..., include_strength=False)`。

这意味着推荐时默认关注阵容本身价值，而不是预测哪支队伍最终更可能获胜。

这个设计非常重要，因为 BP 推荐的目标是帮助用户选阵容，而不是简单押强队。

### 7.3 搜索模块 `BPAgent`

`BPAgent` 是最终推荐器，负责整合策略模型和价值模型。

#### 7.3.1 推荐流程

推荐流程如下：

```text
当前 BPState
    -> PolicyModel 生成候选英雄
    -> 对每个候选英雄模拟落子
    -> ValueModel 评估后续阵容价值
    -> 浅层 minimax 搜索
    -> policy_score 与 value_score 加权
    -> 输出 TopK
```

#### 7.3.2 综合评分

当前综合分为：

```text
score = 0.65 * policy_score + 0.35 * value_score
```

其中：

- `policy_score` 更偏职业 BP 习惯。
- `value_score` 更偏阵容价值和后续搜索。

#### 7.3.3 搜索逻辑

搜索采用浅层 minimax 思路：

- 如果下一手仍是己方行动，则选择让己方价值最大化的分支。
- 如果下一手是敌方行动，则假设敌方会选择对己方最不利的分支。

该搜索深度默认较浅，主要用于在小样本场景下保持速度和稳定性。

## 8. 全局 BP 与赛制规则

项目通过 `kplbp/bp_rules.py` 支持多种 KPL BP 模式。

### 8.1 已支持模式

| 模式 ID | 展示名称 | 最大局数 | 是否全局 BP | 巅峰对决 |
|---|---|---:|---|---|
| `single` | 单局普通 BP | 1 | 否 | 无 |
| `bo3_global` | BO3 全局 BP | 3 | 是 | 无 |
| `bo5_global` | BO5 全局 BP | 5 | 是 | 无 |
| `bo7_global` | BO7 全局 BP | 7 | 是 | 无 |
| `bo5_peak` | BO5 全局 BP + 最后一局巅峰对决 | 5 | 是 | 第 5 局 |
| `bo7_peak` | BO7 全局 BP + 第七局巅峰对决 | 7 | 是 | 第 7 局 |

### 8.2 合法英雄过滤

全局 BP 下，后续局会过滤本方已经 pick 过的英雄。

对于 ban：

- 系统会过滤对方已经无法再 pick 的英雄，避免推荐低价值 ban。

对于 pick：

- 系统会过滤本方已经在前面局使用过的英雄。

### 8.3 巅峰对决

巅峰对决使用无 ban 的 10 手 pick 顺序：

```text
1, 2, 2, 1, 1, 2, 2, 1, 1, 2
```

这意味着系统在巅峰对决局会自动切换 schedule，不再使用常规 20 步 BP。

### 8.4 全局 BP 资源管理

不同系列赛长度会影响英雄资源保留策略。

当前实现中，全局 BP 会对高优先级英雄施加资源保留惩罚：

- BO3：惩罚较轻。
- BO5：惩罚更明显。
- BO7：惩罚最强。

示例逻辑：

```text
早期局剩余局数越多 -> 越不建议轻易消耗最高优先级英雄
后期局剩余局数越少 -> 越接近单局最优策略
```

这使得 BO3、BO5、BO7 在 pick 阶段会表现出不同推荐策略。

## 9. 后端系统设计

后端位于 `kplbp/api.py`，基于 Python 标准库 `http.server` 实现，不依赖复杂 Web 框架。

### 9.1 后端职责

后端主要负责：

- 静态托管 `web/index.html`。
- 加载训练好的 `models/kpl_bp_agent.json`。
- 提供英雄元数据接口。
- 提供阵容分析接口。
- 提供本地 BP 推荐接口。
- 提供云端 AI 复盘接口。
- 提供 LLM 重排序接口。
- 读取云端模型配置。
- 将队伍画像、BP 状态、推荐结果和知识库注入 prompt。

### 9.2 API 列表

#### `GET /api/health`

健康检查接口。

返回：

```json
{
  "ok": true
}
```

#### `GET /api/heroes`

返回英雄元数据，包括英雄列表、分路、职业、伤害类型和标签。

#### `GET /api/bp/config`

返回前端初始化所需配置：

- 英雄元数据。
- 英雄名称列表。
- 默认 BP schedule。
- 支持的赛制模式。
- 分路列表。
- 标签列表。

#### `POST /api/bp/recommend`

核心推荐接口。

输入包括：

- `actions`
- `mode`
- `game_index`
- `camp1_team`
- `camp2_team`
- `camp1_global_used`
- `camp2_global_used`
- `camp1_profile`
- `camp2_profile`
- `top_k`
- `search_depth`

输出包括：

- 当前 `state`
- 当前 `schedule`
- 当前 `strategy_profile`
- 当前 `legal_heroes`
- 推荐列表 `recommendations`
- 是否完成 `done`

推荐列表中每个候选包含：

- `hero`
- `policy_score`
- `value_score`
- `score`
- 全局 BP 下可能包含 `reserve_penalty`
- 全局 BP 下可能包含 `strategy_note`

#### `POST /api/ask`

本地规则分析接口，不调用云端模型。

它基于 `hero_meta.csv` 的标签分析：

- 阵容强点。
- 阵容短板。
- 游戏思路。
- 对敌方阵容的应对。

#### `POST /api/cloud-ai`

云端 AI BP 复盘接口。

它会把以下信息组合进 prompt：

- 用户问题。
- 当前赛制。
- 当前局数。
- 蓝红全局已用英雄。
- 当前 BP 状态。
- 本地模型推荐。
- 英雄标签阵容分析。
- 队伍画像。
- `docs/BP_EXPERIENCE.md` 中的 BP 经验知识库。

#### `POST /api/bp/llm-rerank`

云端 LLM 推荐重排序接口。

用途是对本地模型给出的 Top-10 推荐进行二次判断，并返回：

- 重排后的英雄顺序。
- 每个英雄的理由。
- Top 3 综合分析。
- 下一手核心判断。

该接口用于实现 Agentic Workflow：

```text
本地模型给出候选 -> 云端模型结合队伍画像和 BP 知识做重排序
```

## 10. 云端 AI 配置

项目支持 OpenAI 兼容接口。当前配置文件为：

```json
{
  "base_url": "https://api.deepseek.com/v1",
  "model": "deepseek-chat",
  "rerank_model": "deepseek-chat",
  "api_key": ""
}
```

推荐使用环境变量保存 API Key，而不是写入配置文件：

```bash
export DEEPSEEK_API_KEY="你的 key"
python3 -m kplbp.api
```

后端读取 key 的优先级包括：

- `CLOUD_AI_API_KEY`
- `MOARK_API_KEY`
- `OPENAI_API_KEY`
- `DEEPSEEK_API_KEY`
- `cloud_api_config.json` 中的 `api_key`

实际使用时建议只保留一种 key 来源，避免混淆。

## 11. 前端系统设计

前端位于 `web/index.html`，是一个原生 HTML/CSS/JavaScript 单文件应用。

### 11.1 页面结构

当前前端分为三个主要页面：

- `BP 模拟器`
- `英雄资料库`
- `阵容分析与云端 AI`

### 11.2 BP 模拟器页面

BP 模拟器页面包含：

- 蓝方/红方队伍输入。
- 赛制选择。
- 当前局数选择。
- 蓝方/红方全局已用英雄选择。
- 队伍画像填写。
- 可选英雄池。
- BP 棋盘。
- 时间线。
- 本地模型推荐。
- AI 重排序按钮。
- 云端 AI 分析入口。
- AI 分析预览。

用户可以在页面中按 BP 顺序点击英雄完成模拟，每一步系统会调用 `/api/bp/recommend` 返回新的推荐。

### 11.3 英雄资料库页面

英雄资料库页面用于展示 `hero_meta.csv` 中的英雄信息。

功能包括：

- 按分路分类展示英雄。
- 支持搜索英雄名、分路、职业、伤害类型和标签。
- 展示所有标签。

该页面主要用于检查英雄元数据是否完整，以及辅助用户理解阵容标签。

### 11.4 阵容分析与云端 AI 页面

该页面用于展示完整 AI 分析报告。

此前完整 AI 回答直接挤在 BP 页面右侧，会造成页面布局不平衡。当前已重构为：

- BP 页右侧只保留短预览。
- 完整回答进入独立宽版分析页。
- AI 回答按段落拆分成报告卡片。
- 支持从分析页返回 BP 页面。

这种布局更适合阅读长文本，也不会影响 BP 操作区和推荐区的稳定性。

### 11.5 队伍画像输入

前端支持输入蓝红双方队伍画像：

- 队伍整体风格。
- 胜利条件偏好。
- 五名选手姓名。
- 选手主线路。
- 选手打法风格。
- 选手状态。
- 擅长英雄。
- 回避英雄。

这些信息会随请求一起发送给后端，主要用于云端 AI 和 LLM 重排序。

## 12. 阵容标签分析

`kplbp/analysis.py` 提供本地阵容分析能力。

### 12.1 分析维度

系统会基于英雄元数据统计：

- 分路完整性。
- 职业结构。
- 伤害类型。
- 阵容标签。
- 未知英雄。

### 12.2 强点判断

例如：

- 多个开团/强开标签 -> 具备主动开团能力。
- 多个保护/反打标签 -> 适合围绕核心输出打阵地战。
- 多个消耗/poke/远程标签 -> 适合先压血线再接资源团。
- 有单带标签 -> 可以通过兵线牵制创造空间。

### 12.3 短板判断

例如：

- 缺少某些分路。
- 缺少法术伤害。
- 缺少物理伤害。
- 缺少稳定开团。
- 缺少前排承伤。
- 后期阵容缺少前期节奏点。
- 怕切后排但保护不足。

### 12.4 游戏思路生成

系统会根据标签生成游戏计划，例如：

- 前期阵容建议主动抢线、控野区和争夺资源。
- 后期阵容建议稳线和换资源，保证核心发育。
- 消耗阵容建议提前站位压血线。
- 单带阵容建议利用边线牵制。
- 保护反打阵容建议不要急于先手。

## 13. 训练流程

### 13.1 执行训练

在项目根目录执行：

```bash
python3 -m kplbp.train
```

训练完成后生成：

- `models/kpl_bp_agent.json`
- `reports/audit.json`
- `reports/metrics.json`

### 13.2 默认训练策略

默认只使用完整 20 步 BP 局训练标准 BP 模型。这样可以避免短局数据影响完整 BP 流程学习。

如果希望包含不完整 BP 局：

```bash
python3 -m kplbp.train --include-incomplete
```

### 13.3 样本构造

训练样本分为两类：

#### BP 样本

每一局 BP 会被展开为多个监督学习样本：

```text
第 0 手状态 -> 第 0 手真实英雄
第 1 手状态 -> 第 1 手真实英雄
...
第 19 手状态 -> 第 19 手真实英雄
```

#### 阵容样本

每局比赛会构造蓝方和红方两个阵容样本：

```text
蓝方阵容 vs 红方阵容 -> 蓝方是否获胜
红方阵容 vs 蓝方阵容 -> 红方是否获胜
```

因此当前 415 局完整 BP 可以构造出 830 个阵容样本。

## 14. 当前训练与评估结果

当前 `reports/metrics.json` 中记录了如下训练结果。

### 14.1 样本数量

| 样本类型 | 总数 | 训练集 | 测试集 |
|---|---:|---:|---:|
| BP 样本 | 8300 | 6640 | 1660 |
| 阵容样本 | 830 | 664 | 166 |

### 14.2 策略模型指标

| 指标 | 数值 |
|---|---:|
| test_total | 1660 |
| legal_rate | 1.0000 |
| top1 | 0.1747 |
| top3 | 0.3482 |
| top5 | 0.4416 |

解释：

- `legal_rate = 1.0` 说明测试样本中的真实英雄均在合法候选空间中。
- `top1 = 17.47%` 表示模型第一推荐命中真实职业选择的比例。
- `top5 = 44.16%` 表示真实职业选择出现在前五推荐中的比例。

需要注意，BP 推荐并不是标准分类任务。职业队实际选择不一定是唯一正确答案，因此 Top-K 指标比 Top-1 更有参考意义。

### 14.3 阵容价值模型指标

| 模型 | accuracy | brier | total |
|---|---:|---:|---:|
| 不含队伍强度 | 0.4699 | 0.2483 | 166 |
| 含队伍强度 | 0.5904 | 0.2688 | 166 |

解释：

- 含队伍强度时准确率更高，说明队伍/选手实力对胜负影响明显。
- 不含队伍强度时准确率较低，但它更接近“阵容本身价值”评估，因此更适合 BP 推荐。
- Brier 分数用于衡量概率预测误差，目前仍有优化空间。

### 14.4 特征数量

| 特征类型 | 数量 |
|---|---:|
| player_strengths | 110 |
| team_strengths | 18 |
| team_pick_priors | 18 |
| team_ban_priors | 18 |
| target_ban_priors | 18 |
| value_pair_effects | 826 |
| value_matchup_effects | 1964 |

这些特征说明当前系统已经学习到一定规模的队伍、选手、英雄组合和英雄对位关系。

## 15. 使用方式

### 15.1 启动后端与前端

在项目根目录运行：

```bash
python3 -m kplbp.api
```

默认地址：

```text
http://127.0.0.1:8000
```

### 15.2 配置 DeepSeek

推荐使用环境变量：

```bash
export DEEPSEEK_API_KEY="你的 key"
python3 -m kplbp.api
```

如果后端已经启动，必须先停止旧服务，再重新 export 和启动：

```bash
Ctrl+C
export DEEPSEEK_API_KEY="你的 key"
python3 -m kplbp.api
```

### 15.3 命令行推荐

示例：

```bash
python3 -m kplbp.recommend \
  --order 4 \
  --camp 1 \
  --action-type pick \
  --banned 狄仁杰,鲁班大师,狂铁,盾山 \
  --camp1-team 北京JDG \
  --camp2-team 武汉eStarPro \
  --top-k 5
```

## 16. 当前系统优势

### 16.1 适合小样本数据

系统没有直接使用深度神经网络，而是采用频率模型、统计先验和可解释特征。这种方式更适合当前 419 局左右的数据规模。

### 16.2 考虑队伍/选手影响

项目没有简单把胜负等同于阵容强度，而是加入队伍和选手强度校正，减少强队因素对阵容价值学习的干扰。

### 16.3 支持真实 BP 流程

系统支持逐手 BP 模拟，而不是只在完整阵容后分析。

### 16.4 支持全局 BP

项目已经区分单局 BP 和 BO3/BO5/BO7 全局 BP，并针对不同系列赛长度加入资源管理策略。

### 16.5 支持大模型协同

本地模型负责稳定、快速、可控的候选生成，云端大模型负责解释、重排序和战术分析。二者组合后比单独依赖大模型更稳定。

### 16.6 前端交互完整

当前前端已经支持：

- BP 棋盘。
- 英雄按分路分类。
- 英雄搜索。
- 赛制切换。
- 全局已用英雄选择。
- 队伍画像。
- 模型推荐。
- AI 重排序。
- 云端 AI 分析。
- 英雄资料库。
- 宽版分析报告页。

## 17. 当前限制

### 17.1 数据量仍然有限

当前完整 BP 局数约 415 局，虽然足够做一个可解释 baseline，但对于复杂模型仍偏少。

### 17.2 版本漂移问题

KPL 英雄强度随版本变化明显。跨版本数据会带来噪声，旧版本强势英雄可能不适合当前版本。

### 17.3 阵容价值模型仍较弱

不含队伍强度的阵容价值模型测试准确率约 46.99%，说明仅凭当前样本学习阵容胜负仍有困难。

可能原因：

- 样本少。
- 阵容胜负受执行影响大。
- 英雄强度随版本变化。
- 队伍战术体系未完全建模。
- 局内经济、龙团、前期节奏等信息未纳入。

### 17.4 英雄标签依赖人工维护

`hero_meta.csv` 的质量直接影响阵容分析和云端 prompt。若标签缺失或不准确，分析结果也会受影响。

### 17.5 云端 AI 依赖外部服务

云端 AI 分析依赖 API Key、网络连接和模型服务可用性。若接口不可用，本地推荐仍可运行，但解释和重排序能力会下降。

### 17.6 目前还不是真正强化学习 Agent

当前系统是监督学习 + 价值评估 + 搜索 + LLM 辅助，不是完整的强化学习自博弈系统。

## 18. 优化方向

### 18.1 数据层优化

后续可以补充：

- 更细版本号。
- 版本对应英雄调整信息。
- 每局蓝红方信息。
- 更明确的首抢/反制/体系标签。
- 队伍近期状态。
- 选手近期英雄池。
- 局内关键事件数据，如一血、暴君、主宰、十分钟经济差。

### 18.2 模型层优化

可以继续优化：

- 为不同版本训练独立模型。
- 引入时间衰减权重，使近期比赛更重要。
- 使用 LightGBM / CatBoost 做阵容价值模型。
- 对英雄 pair 和 matchup 加入正则化。
- 对 BP 阶段训练单独模型，例如首轮 ban、首抢、二三楼、四五楼。
- 对不同赛制训练独立策略头。
- 结合选手英雄熟练度训练个性化推荐分。

### 18.3 搜索层优化

当前搜索深度较浅。后续可以：

- 使用更智能的候选剪枝。
- 对不同 BP 阶段设置不同搜索宽度。
- 在全局 BP 下加入跨局资源规划。
- 对 ban 和 pick 使用不同价值函数。
- 将阵容完整性约束直接加入搜索评分。

### 18.4 云端 AI 优化

可以进一步：

- 让 LLM 输出更严格 JSON。
- 将云端分析拆成多个专家角色，如阵容教练、版本教练、数据分析师。
- 对 AI 重排序结果进行缓存。
- 将本地模型分数、英雄标签、队伍画像和历史对局摘要结构化输入。
- 支持多模型对比，例如 DeepSeek、Qwen、GPT 等。

### 18.5 前端优化

可以继续提升：

- 英雄头像展示。
- BP 动画。
- 推荐理由可视化。
- 赛制进度管理。
- 多局全局 BP 历史记录。
- 自动保存当前 BP 方案。
- 导出 BP 报告。
- 队伍画像模板。
- 英雄标签编辑器。

## 19. 项目运行流程总结

完整流程如下：

```text
1. 准备 CSV 数据
   - kpl_bp.csv
   - kpl_players.csv
   - KPL_hero_2023_2026.csv
   - hero_meta.csv

2. 执行训练
   python3 -m kplbp.train

3. 生成模型与报告
   - models/kpl_bp_agent.json
   - reports/audit.json
   - reports/metrics.json

4. 启动后端
   python3 -m kplbp.api

5. 打开前端
   http://127.0.0.1:8000

6. 选择赛制和当前局

7. 在前端模拟 BP

8. 本地模型返回推荐

9. 可选：调用 LLM 重排序

10. 可选：调用云端 AI 生成完整 BP 分析报告
```

## 20. 结论

本项目已经形成一个较完整的 KPL BP 推荐系统原型。它不是简单的英雄胜率表，也不是完全依赖大模型的聊天工具，而是将职业 BP 历史、阵容价值建模、队伍选手校正、全局 BP 规则和云端大模型分析结合起来。

在当前数据规模有限、版本有效期较短的约束下，项目选择了可解释、可调试、稳定性较高的建模路线。当前模型的 Top-5 BP 行为命中率约 44.16%，可以作为 BP 辅助推荐的基础版本。阵容价值模型仍有较大提升空间，但通过队伍/选手强度校正，系统已经初步解决了“强队拿弱阵容也能赢”带来的训练偏差问题。

后续如果继续补充更细粒度版本数据、选手英雄池数据、局内事件数据和更高质量的英雄标签，本项目可以进一步向更专业的 KPL BP 分析平台演进。
